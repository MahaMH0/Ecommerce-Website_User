import { EgyptionIDParsePipe } from './egyption-idparse.pipe';

describe('EgyptionIDParsePipe', () => {
  it('create an instance', () => {
    const pipe = new EgyptionIDParsePipe();
    expect(pipe).toBeTruthy();
  });
});
